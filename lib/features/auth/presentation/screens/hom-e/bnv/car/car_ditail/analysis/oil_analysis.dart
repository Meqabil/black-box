import 'package:flutter/material.dart';

class OilAnalysisScreen extends StatelessWidget {
  const OilAnalysisScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Oil Analysis Screen"),
      ),
    );
  }
}