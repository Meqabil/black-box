import 'package:flutter/material.dart';

class AccelerationAnalysisScreen extends StatelessWidget {
  const AccelerationAnalysisScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Acceleration Analysis Screen"),
      ),
    );
  }
}