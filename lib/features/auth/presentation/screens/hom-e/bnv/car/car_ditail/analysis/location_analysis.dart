import 'package:flutter/material.dart';

class LocationAnalysisScreen extends StatelessWidget {
  const LocationAnalysisScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Location Analysis Screen"),
      ),
    );
  }
}