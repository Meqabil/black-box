import 'package:flutter/material.dart';

class SpeedAnalysisScreen extends StatelessWidget {
  const SpeedAnalysisScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Speed Analysis Screen"),
      ),
    );
  }
}