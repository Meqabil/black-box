import 'package:flutter/material.dart';

class CoolantTemp extends StatelessWidget {
  const CoolantTemp({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Acceleration Analysis Screen"),
      ),
    );
  }
}