import 'package:flutter/material.dart';

class LongitudeAnalysisScreen extends StatelessWidget {
  const LongitudeAnalysisScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Longitude Analysis Screen"),
      ),
    );
  }
}