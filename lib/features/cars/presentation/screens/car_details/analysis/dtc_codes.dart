import 'package:flutter/material.dart';

class DtcCodes extends StatelessWidget {
  const DtcCodes({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Latitude Analysis Screen"),
      ),
    );
  }
}