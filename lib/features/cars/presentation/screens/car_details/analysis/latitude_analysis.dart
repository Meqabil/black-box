import 'package:flutter/material.dart';

class LatitudeAnalysisScreen extends StatelessWidget {
  const LatitudeAnalysisScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Latitude Analysis Screen"),
      ),
    );
  }
}