import 'package:flutter/material.dart';

class DrivingEventsScreen extends StatelessWidget {
  const DrivingEventsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Driving Events Screen"),
      ),
    );
  }
}
